<script>
            alert('本の詳細ページだよ');
</script>
<h1>git practice
</h1>
<div class="container">
	<div class="row">
		<div class="col-lg-4">
			<%= render 'layouts/user_info', user: @user %>
            <%= render 'books/new',book: @booker %>
        </div>
		<div class="col-lg-8">
			<h1>Book detail</h1>

                <table class="table">
	               <td><%=@book.user.name %></td>
	               <td><%=@book.title %></td>
	               <td><%=@book.body %></td>
	               <td><%=@book.category %></td>
                 <%if @book.user == current_user %>
                   <td><%= link_to "Edit", edit_book_path(@book.id) %></td>
	               <td><%= link_to "Destroy", book_path(@book),method: :delete %></td>
                 <% end %>
                </table>
        </div>
    </div>
</div>

<h2>あああ</h2>

<h3>１２３４５</h3>
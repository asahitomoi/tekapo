<h1>User info</h1>
<%= attachment_image_tag user,:image, fallback: "no_img.jpg",size:'20×20' %>
<table class="table">
  <tr>
	<td>name</td>
	<td><%=user.name %></td>
  </tr>
  <tr>
  	<td>introduction</td>
  	<td><%=user.body %></td>
  </tr>
</table>

<%= link_to  edit_user_path(user.id), class: "btn btn-primary btn-block edit_user_#{user.id}" do %><span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
<% end %>